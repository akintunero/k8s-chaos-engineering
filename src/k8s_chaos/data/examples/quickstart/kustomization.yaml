apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: hello-world-app

resources:
  - namespace.yaml
  - app.yaml
  - rbac.yaml

labels:
  - pairs:
      app.kubernetes.io/part-of: k8s-chaos-engineering
      app.kubernetes.io/component: quickstart
